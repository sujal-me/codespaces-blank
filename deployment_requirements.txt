# Deployment Requirements for Render
# Chest X-Ray Report Generator API
# MEMORY OPTIMIZED VERSION - Fits under 512MB RAM

# Core Flask
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0
python-dotenv==1.0.0

# ML/Deep Learning (CPU only)
# Use a compatible PyTorch (CPU) series; prefer a flexible range so newer CPU wheels are installed.
--extra-index-url https://download.pytorch.org/whl/cpu
# Use a semantically compatible version range, avoid strict +cpu pins which may not be published.
torch>=2.2.0,<3.0.0
torchvision>=0.17.0,<1.0.0

# Image Processing
Pillow==10.1.0
albumentations==1.3.1
numpy==1.26.2

# PDF Generation
reportlab==4.0.7

# Enhanced Features (lazy loaded to save memory)
transformers==4.35.2
google-generativeai==0.3.1
huggingface-hub==0.19.4
accelerate

# NOTE: No spacy, no sklearn - removed to save memory
# GPT-2 is lazy loaded (not kept in memory)
